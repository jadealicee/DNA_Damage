#artemis

conda config --add channels bioconda     #(to add the bioconda channel)

conda config --add channels conda-forge  #(to add the conda-forge channel)

#install artemis into channel
conda install artemis

#run artemis
art